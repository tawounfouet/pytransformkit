[build-system]
requires = ["hatchling>=1.25,<2"]
build-backend = "hatchling.build"

[project]
name = "pytransformkit"
version = "0.1.0a1"
description = "Engine-agnostic data transformation framework for Python."
readme = "README.md"
requires-python = ">=3.11"
authors = [
  { name = "Thomas Awounfouet" },
]
keywords = [
  "data",
  "data-engineering",
  "transformation",
  "pandas",
  "polars",
  "pyarrow",
  "duckdb",
]
classifiers = [
  "Development Status :: 2 - Pre-Alpha",
  "Intended Audience :: Developers",
  "Intended Audience :: Science/Research",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Typing :: Typed",
]
dependencies = []

[project.optional-dependencies]
dev = [
  "build>=1.2,<2",
  "hypothesis>=6,<7",
  "mypy>=1.15,<2",
  "pre-commit>=4,<5",
  "pytest>=8,<10",
  "pytest-cov>=6,<8",
  "ruff>=0.11,<1",
]
pandas = [
  "pandas>=2.2,<4",
]
polars = [
  "polars>=1.0,<2",
]

[project.urls]
Repository = "https://github.com/tawounfouet/pytransformkit"
Issues = "https://github.com/tawounfouet/pytransformkit/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/pytransformkit"]

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.11"
mypy_path = "src"
warn_unused_configs = true
disallow_untyped_defs = true
check_untyped_defs = true
no_implicit_optional = true
show_error_codes = true

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
markers = [
  "unit: fast isolated tests",
  "architecture: architectural boundary tests",
  "contract: shared contract tests",
  "cross_engine: logical equivalence tests across engines",
  "integration: multi-component integration tests",
  "performance: benchmark or performance tests",
  "external: tests requiring external services",
  "slow: slow-running tests",
]

[tool.coverage.run]
source = ["pytransformkit"]
branch = true

[tool.coverage.report]
show_missing = true
skip_covered = true

[[tool.mypy.overrides]]
module = ["pandas", "pandas.*", "polars", "polars.*"]
ignore_missing_imports = true
